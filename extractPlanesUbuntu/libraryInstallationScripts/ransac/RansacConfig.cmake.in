# - Config file for the FooBar package
# It defines the following variables
#  RANSAC_INCLUDE_DIRS - include directories for ransac
#  RANSAC_LIBRARIES    - libraries to link against

# Compute paths
get_filename_component(RANSAC_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(RANSAC_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# These are IMPORTED targets created by FooBarTargets.cmake
set(RANSAC_LIBRARIES ransac)

