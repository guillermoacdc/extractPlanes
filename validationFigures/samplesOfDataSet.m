% create a figure for three sessions, with the next components in each
% session-keyframe ()
% 1. RGB image
% 2. color point cloud image
% 3. non colored point cloud with visible planes annotated

sessionID=10;
frameID= 24;

% load rgb image

% load color point cloud

% load visible planes with noncolored point cloud